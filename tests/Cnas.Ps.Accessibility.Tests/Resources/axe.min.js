// PLACEHOLDER — replace with the real axe-core 4.10 minified bundle at CI setup time.
//
// At scan time the CI accessibility job runs:
//   curl -L https://github.com/dequelabs/axe-core/releases/download/v4.10.0/axe.min.js \
//        -o tests/Cnas.Ps.Accessibility.Tests/Resources/axe.min.js
//
// Locally, leaving this placeholder in place causes AxeRunner.RunAsync to throw
// AxeBundleMissingException, which the Playwright theory tests translate into a
// Skip="axe-core bundle missing" — so offline dev runs do not fail spuriously.
//
// The literal sentinel "// PLACEHOLDER" on the very first line is the detection token;
// do NOT remove or rename it without updating AxeRunner.IsPlaceholderBundle.
window.__cnasAxePlaceholder = true;
