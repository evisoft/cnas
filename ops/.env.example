# Sample environment for local development. Copy to ops/.env and customise.
# Never commit a real ops/.env — see .gitignore.

POSTGRES_PASSWORD=cnas
MINIO_ROOT_USER=cnas
MINIO_ROOT_PASSWORD=cnaspassword
